# DSH WebUI 功能对等矩阵

| 能力 | 插件 API | Android | 状态 |
| --- | --- | --- | --- |
| 健康检查与一次性配对 | 已有 | Android 15 发布版已通过真实 HTTP listener 验收；HTTPS 待验收 | in-progress |
| 设备身份、scope 与根授权 | 已有 | Android 15 已读取真实设备、7 scope 与授权根 | in-progress |
| 会话列表与创建 | 已有，含 DSH 工作区发现/创建 | 可选择已有工作区，或从授权根浏览工作目录、登记新 DSH 工作区并创建会话 | done |
| 历史、发送、steer、取消 | 已有 | 历史投影与 Markdown 已完成，真实消息发送/回复已通过；steer 与取消仍待真实运行验收 | in-progress |
| 聊天实时事件 | 已有稳定文本/思考 delta | 真实 Oracle listener 已验证 App 按 token 连续增长并在完成后用 REST 历史收口；网络切换待验收 | in-progress |
| 文件列表、读写和 ETag | 已有 | 已实现行号文本编辑、Markdown 源码/预览与 ETag 保存；冲突仍待端到端验收 | in-progress |
| 新建、移动、上传、下载、软删除、恢复 | 已有 | 已实现并有 MockWebServer/服务端回归，移动 UI 动作待验收 | in-progress |
| 会话搜索、重命名与分叉 | 重命名与分叉已有；搜索未进入 v1 | 会话子项菜单可重命名并分叉，分叉后自动进入子会话 | in-progress |
| 模型与推理等级 | 已有 | 已读取真实模型组、切换模型与服务端支持的思考强度 | done |
| Agent Preset / 模式选择 | 已有列表、创建参数及空白会话切换 | 新建和现有空白会话可切换；首条消息后按 DSH 规则显示锁定 | done |
| 当前 TODO / 任务模块 | 历史 `projections.values.todos` | 聊天页显示完成进度和可折叠任务列表，随历史刷新 | done |
| 斜杠命令 | 已有当前会话 host 命令列举/执行 BFF | 输入 `/` 即筛选、补全并受控执行，不作为模型消息发送 | done |
| 会话权限预设 | 已有 `permissions` 投影及 `/permission` 命令 | 输入区可切换只读、工作区写入和完整访问；完整访问二次确认 | done |
| 附件与消息队列 | 缺失 | 等待 BFF | planned |
| 工具详情、审批与问题回答 | 审批 BFF 已有；通用问题回答缺失 | App 已显示待审批状态、脱敏命令、“允许一次/拒绝”及完整访问二次确认；真实服务器重载与问题回答仍待后续 | in-progress |
| DSH 工作区发现与会话归属 | 已有，按授权根过滤 | 已实现选择和会话标题/工作区标题显示 | done |
| DSH 工作区创建 | 已有，限授权目录 | App 目录选择器可登记并立即用于新会话 | done |
| DSH 工作区管理与会话归档 | 工作区重命名/移除及会话归档已有 | 工作区管理页可重命名/移除，会话子项可归档；自定义排序和归档恢复待后续 | in-progress |
| 目标、子代理、技能和后台任务 | 缺失 | 等待 BFF | planned |
| Provider、凭据与设置 | 已有细粒度 scope、有效配置投影和自定义创建 | 可查看已填配置/继承模型、编辑配置、只写/清除凭据、发现模型并创建完全自定义供应商 | done |

“对等”要求 App 能读取同一状态并执行同一用户动作；仅显示 WebView 或直接调用 DSH 私有 `/api` 不计为完成。
